<?xml version="1.0" encoding="utf-8"?>
<resources>
    <dimen name="dialpad_button_size">68dp</dimen>
    <dimen name="dialpad_button_size_small">50dp</dimen>
    <dimen name="incoming_call_arrow_size">50dp</dimen>
    <dimen name="incoming_call_button_size">72dp</dimen>
    <dimen name="call_notification_button_size">40dp</dimen>
    <dimen name="caller_avatar_max_size">172dp</dimen>
    <dimen name="end_call_button_margin">34dp</dimen>
    <dimen name="dialpad_bottom_padding">114dp</dimen>
    <dimen name="call_control_max_height">240dp</dimen>
    <dimen name="call_control_button_size">72dp</dimen>
    <dimen name="recyclerview_fab_padding">100dp</dimen>

    <!-- keeps the (now square) favorites call buttons visually consistent -->
    <dimen name="call_button_corner_radius">8dp</dimen>
    <!-- shrunk + condensed action buttons -->
    <dimen name="call_button_size">44dp</dimen>
    <dimen name="star_button_size">44dp</dimen>

    <!-- make contact face-icons bigger and square everywhere (overrides commons) -->
    <dimen name="list_icon_size_medium">52dp</dimen>

    <!-- clears the fast-scroller on the left so list items don't sit under the letters -->
    <dimen name="contacts_list_start_padding">44dp</dimen>

    <!-- FAB positioning: inset from left edge past the fast scroller and raised above tabs -->
    <dimen name="fab_start_margin">76dp</dimen>
    <dimen name="fab_bottom_margin">24dp</dimen>
    <dimen name="fab_custom_size">60dp</dimen>

    <dimen name="caller_name_text_size">34sp</dimen>
    <dimen name="call_status_text_size">20sp</dimen>

    <!-- keep these 2 values in dp to avoid scaling -->
    <dimen name="dialpad_text_size">28dp</dimen>
    <dimen name="dialpad_letters_text_size">12dp</dimen>

    <!-- fast scroller letter size (shrunk so they fit on narrow screens) -->
    <dimen name="fast_scroller_letter_size">9sp</dimen>

    <!-- bottom nav bars: BOTH use this exact fixed height so dialer is pixel-identical to main -->
    <dimen name="bottom_nav_min_height">52dp</dimen>
</resources>
